name: Deploy Frontend to S3

on:
  push:
    branches: ["*"]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # מגדירים הרשאות AWS (עדיף ככה מאשר להתקין awscli ידנית)
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ secrets.AWS_REGION }}

      # אין build – זה אתר סטטי
      - name: Skip build (static site)
        run: echo "Nothing to build"

      # סנכרון תיקיית ה-root של הריפו ל-S3
      - name: Sync to S3
        run: |
          aws s3 sync . s3://${{ secrets.S3_BUCKET_NAME }} \
            --delete \
            --exclude ".git/*" \
            --exclude ".github/*" \
            --exclude "README.md"

      # אופציונלי: Invalidation ל-CloudFront (אם יש)
      # הוסף סיקרט בשם CLOUDFRONT_DISTRIBUTION_ID אם תרצה ולהפעיל את הבלוק:
      # - name: Invalidate CloudFront
      #   if: ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID != '' }}
      #   run: |
      #     aws cloudfront create-invalidation \
      #       --distribution-id ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }} \
      #       --paths "/*"
